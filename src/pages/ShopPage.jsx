import FilterBar from '../components/FilterBar.jsx'
import ProductCard from '../components/ProductCard.jsx'

function ShopPage({ products, categories, filteredProducts, isLoading, errorMessage, searchTerm, selectedCategory, minRating, maxPrice, sortBy, onSearchChange, onCategoryChange, onRatingChange, onMaxPriceChange, onSortChange, onRetry, onAddToCart }) {
  return <main className="mx-auto max-w-7xl px-5 py-14 sm:px-8"><div className="flex flex-wrap items-end justify-between gap-5"><div><p className="text-sm font-bold uppercase tracking-[0.2em] text-blue-700">The full collection</p><h1 className="mt-3 text-5xl font-bold tracking-tight text-blue-950">Shop everything</h1><p className="mt-4 max-w-xl text-slate-600">Find your next everyday essential, from practical upgrades to considered gifts.</p></div><p className="text-sm text-slate-500">{isLoading ? 'Loading...' : `${filteredProducts.length} results`}</p></div><div className="mt-8"><FilterBar categories={categories} selectedCategory={selectedCategory} minRating={minRating} maxPrice={maxPrice} sortBy={sortBy} onCategoryChange={onCategoryChange} onRatingChange={onRatingChange} onMaxPriceChange={onMaxPriceChange} onSortChange={onSortChange} /></div>{isLoading && <p className="py-24 text-center text-slate-500">Loading products...</p>}{errorMessage && <div className="mt-8 rounded-2xl border border-red-200 bg-red-50 p-6 text-center text-red-700"><p>{errorMessage}. Please try again.</p><button className="mt-4 rounded-lg bg-blue-700 px-4 py-2 text-sm font-bold text-white" type="button" onClick={onRetry}>Retry</button></div>}{!isLoading && !errorMessage && !filteredProducts.length && <p className="mt-8 rounded-2xl border border-dashed border-slate-300 p-16 text-center text-slate-500">No products found for “{searchTerm}”.</p>}{!isLoading && !errorMessage && filteredProducts.length > 0 && <div className="mt-8 grid grid-cols-2 gap-3 sm:gap-5 lg:grid-cols-4">{filteredProducts.map((product) => <ProductCard key={product.id} product={product} onAddToCart={onAddToCart} />)}</div>}</main>
}

export default ShopPage
